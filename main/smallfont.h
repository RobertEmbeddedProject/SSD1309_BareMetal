#pragma once                                    //preprocessor directive to ensure everything here is only compiled once, no matter what

extern const uint8_t font8x8_basic[96][8];
//extern: Declare the variable elsewhere
